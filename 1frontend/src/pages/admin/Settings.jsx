const Settings = () => {
    return (
        <section className="admin-page">
            <h1>Settings</h1>
            <p>Administrative settings will be added here.</p>
        </section>
    );
};

export default Settings;
