const Reservations = () => {
    return (
        <section className="admin-page">
            <h1>Reservations</h1>
            <p>Reservation management will be added here.</p>
        </section>
    );
};

export default Reservations;
