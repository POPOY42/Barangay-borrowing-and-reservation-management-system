const Profile = () => {
    return (
        <section className="admin-page">
            <h1>Profile</h1>
            <p>Admin profile details will be added here.</p>
        </section>
    );
};

export default Profile;
